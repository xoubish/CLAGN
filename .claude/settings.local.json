{
  "permissions": {
    "allow": [
      "Bash(git init:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(python -c \":*)"
    ],
    "deny": [
      "Read(//Users/shemmati/.netrc)",
      "Read(~/.netrc)",
      "Read(//Users/shemmati/.netrc.gpg)",
      "Read(~/.netrc.gpg)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -qi 'netrc' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: this command touches ~/.netrc, which holds SDSS-V collaboration credentials. Read or edit it in your own terminal, not through Claude.\"}}' || true",
            "statusMessage": "checking for credential access"
          }
        ]
      }
    ]
  }
}
